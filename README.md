# Тестовое задание. Сервис для осуществления финансовых операций.

Параметры для запуска сервера и БД указываются в файле ".env". При пустом файле загружаются значения по умолчанию (8080 порт).

Запуск приложения:
```sh
make run
```
Запуск тестов:
```sh
make tests
```
